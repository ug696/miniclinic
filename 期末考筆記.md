# MiniClinic 期末考筆記

這份筆記是針對本專案 `/Users/ug/Desktop/miniclinic` 整理的。目標是讓完全初學者也能從最基本的地方開始理解，並且知道期末考可能會問什麼、怎麼回答。

---

## 0. 先用一句話理解這個專案

MiniClinic 是一個用 **Spring Boot** 做的簡易診所系統。

它可以：

- 顯示首頁
- 顯示醫師清單
- 依科別篩選醫師
- 查看單一醫師詳細資料
- 顯示病患清單
- 線上掛號
- 顯示掛號紀錄
- 顯示統計資訊
- 提供 REST API
- 醫師登入
- 登入後進入 Dashboard
- 醫師只能修改自己掛號的狀態
- 醫師可以修改密碼

專案主要使用：

- Java 17
- Spring Boot
- Spring MVC
- Thymeleaf
- Spring Data JPA
- SQLite
- Bean Validation
- Session
- Interceptor
- BCrypt

---

## 1. 最基礎觀念

### 1.1 什麼是 Web？

Web 系統通常有兩邊：

- 瀏覽器：使用者看到的畫面，例如首頁、表單、清單。
- 伺服器：處理請求、查資料庫、回傳網頁或 JSON。

使用者在瀏覽器輸入網址或按按鈕時，瀏覽器會送出 HTTP Request。伺服器收到後，處理完再回 HTTP Response。

例子：

```text
使用者打開 /doctors
瀏覽器送出 GET /doctors
Spring Boot 找到 DoctorPageController
Controller 查詢 doctorRepo.findAll()
把 doctors 放進 Model
回傳 doctors.html
Thymeleaf 把資料套進 HTML
瀏覽器顯示醫師清單
```

### 1.2 HTTP Method

常考：

| Method | 意義 | 本專案例子 |
|---|---|---|
| GET | 查詢資料或顯示頁面 | `GET /doctors` |
| POST | 新增資料或送出表單 | `POST /appointment/new` |
| PUT | 修改資料 | `PUT /api/appointments/{apptId}/status` |
| DELETE | 刪除資料 | `DELETE /api/doctors/{doctorId}` |

### 1.3 HTTP 狀態碼

| 狀態碼 | 意義 | 本專案例子 |
|---|---|---|
| 200 | 成功 | 查詢醫師成功 |
| 201 | 新增成功 | `POST /api/doctors` |
| 204 | 成功但沒有內容 | 刪除醫師成功 |
| 400 | 請求格式錯誤 | 狀態不是 BOOKED/COMPLETED/CANCELLED |
| 401 | 未登入 | API 被 Interceptor 擋下 |
| 403 | 沒有權限 | 醫師修改別人的掛號 |
| 404 | 找不到資料 | 查 `D999` |

---

## 2. 專案結構

重要檔案位置：

```text
pom.xml
src/main/java/tw/edu/fju/miniclinic/
  MiniclinicApplication.java
  config/WebConfig.java
  interceptor/LoginRequiredInterceptor.java
  controller/
  model/
src/main/resources/
  application.properties
  data.sql
  templates/
```

### 2.1 `pom.xml`

`pom.xml` 是 Maven 專案設定檔，用來管理套件依賴。

本專案重要依賴：

| 依賴 | 用途 |
|---|---|
| `spring-boot-starter-web` | 建立 Web、Controller、REST API |
| `spring-boot-starter-thymeleaf` | 使用 Thymeleaf 產生 HTML |
| `spring-boot-starter-data-jpa` | 使用 JPA 操作資料庫 |
| `sqlite-jdbc` | 連接 SQLite |
| `hibernate-community-dialects` | 讓 Hibernate 支援 SQLite |
| `spring-boot-starter-validation` | 表單驗證，例如 `@NotBlank` |
| `spring-security-crypto` | 使用 BCrypt 加密密碼 |
| `spring-boot-devtools` | 開發時自動重啟 |

---

## 3. Spring Boot 啟動點

檔案：

```text
src/main/java/tw/edu/fju/miniclinic/MiniclinicApplication.java
```

重點程式：

```java
@SpringBootApplication
public class MiniclinicApplication {
    public static void main(String[] args) {
        SpringApplication.run(MiniclinicApplication.class, args);
    }
}
```

### 必懂

`@SpringBootApplication` 是 Spring Boot 的入口註解，包含：

- 自動設定
- 元件掃描
- Spring Boot 啟動設定

`SpringApplication.run(...)` 會啟動整個 Spring Boot 應用程式，也會啟動內嵌 Tomcat。

---

## 4. MVC 架構

MVC 是：

| 名稱 | 意義 | 本專案例子 |
|---|---|---|
| Model | 資料與商業邏輯 | `Doctor`, `Patient`, `Appointment` |
| View | 使用者看到的畫面 | `doctors.html`, `login.html` |
| Controller | 接收請求、決定要做什麼 | `DoctorPageController` |

範例流程：

```text
GET /doctors
-> DoctorPageController
-> doctorRepo.findAll()
-> model.addAttribute("doctors", doctors)
-> return "doctors"
-> templates/doctors.html
```

### `@Controller` vs `@RestController`

| 註解 | 回傳內容 | 用途 |
|---|---|---|
| `@Controller` | 回傳 HTML 頁面名稱 | 給人看的網頁 |
| `@RestController` | 回傳 JSON | 給程式或前端 API 使用 |

例子：

- `HomeController` 是 `@Controller`，回傳 `home.html`
- `DoctorApiController` 是 `@RestController`，回傳 JSON

---

## 5. Thymeleaf 重點

Thymeleaf 是模板引擎，用來把 Java 後端資料放進 HTML。

HTML 頁面通常在：

```text
src/main/resources/templates/
```

### 常用語法

| 語法 | 用途 | 例子 |
|---|---|---|
| `th:text` | 替換文字 | `th:text="${doctor.name}"` |
| `th:each` | 迴圈 | `th:each="doctor : ${doctors}"` |
| `th:if` | 條件成立才顯示 | `th:if="${error}"` |
| `th:unless` | 條件不成立才顯示 | `th:unless="${doctors.isEmpty()}"` |
| `th:href` | 產生連結 | `th:href="@{/doctors}"` |
| `th:object` | 綁定表單物件 | `th:object="${form}"` |
| `th:field` | 綁定欄位 | `th:field="*{chartNo}"` |
| `th:errors` | 顯示驗證錯誤 | `th:errors="*{chartNo}"` |

### Model 是什麼？

Controller 用 `Model` 把資料傳給 HTML。

```java
model.addAttribute("doctors", doctorRepo.findAll());
return "doctors";
```

HTML 就能使用：

```html
<tr th:each="doctor : ${doctors}">
    <td th:text="${doctor.name}"></td>
</tr>
```

---

## 6. Entity、Repository、資料庫

### 6.1 Entity 是什麼？

Entity 是 Java 類別，對應資料庫的資料表。

本專案有三個主要 Entity：

| Entity | 資料表 | 主鍵 |
|---|---|---|
| `Doctor` | `doctor` | `doctor_id` |
| `Patient` | `patient` | `chart_no` |
| `Appointment` | `appointment` | `appt_id` |

### 6.2 常用 JPA 註解

| 註解 | 意義 |
|---|---|
| `@Entity` | 這個類別是資料庫 Entity |
| `@Table(name = "...")` | 指定資料表名稱 |
| `@Id` | 主鍵 |
| `@Column` | 對應資料表欄位 |
| `@GeneratedValue` | 主鍵自動產生 |
| `@ManyToOne` | 多筆資料對一筆資料 |
| `@JoinColumn` | 外鍵欄位 |
| `@Convert` | 自訂型別轉換 |

### 6.3 Doctor

`Doctor` 對應醫師資料。

重要欄位：

| Java 欄位 | 資料庫欄位 | 意義 |
|---|---|---|
| `doctorId` | `doctor_id` | 醫師編號 |
| `name` | `name` | 姓名 |
| `department` | `department` | 科別 |
| `specialty` | `specialty` | 專長 |
| `passwordHash` | `password_hash` | 密碼雜湊 |

`passwordHash` 有 `@JsonIgnore`，代表 API 回傳 JSON 時不會把密碼雜湊送出去。

這很重要，因為密碼資料不應該暴露。

### 6.4 Patient

`Patient` 對應病患資料。

重要欄位：

| Java 欄位 | 資料庫欄位 | 意義 |
|---|---|---|
| `chartNo` | `chart_no` | 病歷號 |
| `name` | `name` | 姓名 |
| `gender` | `gender` | 性別 |
| `birthDate` | `birth_date` | 生日 |
| `phone` | `phone` | 電話 |

### 6.5 Appointment

`Appointment` 對應掛號資料。

重要欄位：

| Java 欄位 | 資料庫欄位 | 意義 |
|---|---|---|
| `apptId` | `appt_id` | 掛號編號 |
| `patient` | `chart_no` | 病患 |
| `doctor` | `doctor_id` | 醫師 |
| `apptDate` | `appt_date` | 掛號日期 |
| `timeSlot` | `time_slot` | 時段 |
| `status` | `status` | 狀態 |

### 6.6 `@ManyToOne`

Appointment 裡有：

```java
@ManyToOne
@JoinColumn(name = "chart_no", nullable = false)
private Patient patient;

@ManyToOne
@JoinColumn(name = "doctor_id", nullable = false)
private Doctor doctor;
```

意思是：

- 多筆掛號可以屬於同一位病患
- 多筆掛號可以屬於同一位醫師

資料庫關係：

```text
doctor 1 ---- many appointment
patient 1 --- many appointment
```

---

## 7. Repository

Repository 是資料庫操作介面。

本專案：

```text
DoctorRepository extends JpaRepository<Doctor, String>
PatientRepository extends JpaRepository<Patient, String>
AppointmentRepository extends JpaRepository<Appointment, Long>
```

`JpaRepository<Entity, 主鍵型別>`。

### 7.1 JpaRepository 內建方法

常用：

| 方法 | 用途 |
|---|---|
| `findAll()` | 查全部 |
| `findById(id)` | 用主鍵查一筆 |
| `save(entity)` | 新增或更新 |
| `deleteById(id)` | 刪除 |
| `existsById(id)` | 判斷是否存在 |
| `count()` | 計算筆數 |

### 7.2 方法命名查詢

Spring Data JPA 可以靠方法名稱自動產生查詢。

例子：

```java
List<Doctor> findByDepartment(String department);
List<Appointment> findByApptDate(LocalDate apptDate);
List<Appointment> findByDoctorAndApptDate(Doctor doctor, LocalDate apptDate);
```

意思：

- `findByDepartment`：依科別查醫師
- `findByApptDate`：依日期查掛號
- `findByDoctorAndApptDate`：依醫師和日期查掛號

### 7.3 `@Query`

`DoctorRepository` 有：

```java
@Query("select distinct d.department from Doctor d")
List<String> findAllDepartments();
```

意思是查出所有不重複的科別。

`distinct` 是去除重複。

---

## 8. SQLite 與 `application.properties`

重要設定：

```properties
spring.datasource.url=jdbc:sqlite:miniclinic.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
```

### 必懂

| 設定 | 意義 |
|---|---|
| `server.port=8080` | 網站跑在 8080 port |
| `jdbc:sqlite:miniclinic.db` | SQLite 資料庫檔案叫 `miniclinic.db` |
| `ddl-auto=update` | 啟動時自動建立或更新資料表 |
| `show-sql=true` | 顯示 Hibernate 執行的 SQL |
| `defer-datasource-initialization=true` | 先建立資料表，再執行 `data.sql` |
| `spring.sql.init.mode=always` | 每次啟動都執行 `data.sql` |

### `data.sql`

`data.sql` 用來放初始資料。

本專案放了：

- 5 位醫師
- 3 位病患
- 3 筆掛號

使用：

```sql
INSERT OR IGNORE INTO ...
```

`OR IGNORE` 的意思是如果資料已經存在，就不要重複插入，避免主鍵衝突。

---

## 9. LocalDateConverter

SQLite 沒有真正的日期型別，所以本專案用 `LocalDateConverter` 把日期轉成字串存進資料庫。

```java
public String convertToDatabaseColumn(LocalDate date) {
    return date == null ? null : date.toString();
}

public LocalDate convertToEntityAttribute(String s) {
    return s == null ? null : LocalDate.parse(s);
}
```

轉換：

```text
Java LocalDate  ->  "2026-05-01"
"2026-05-01"   ->  Java LocalDate
```

搭配：

```java
@Convert(converter = LocalDateConverter.class)
```

---

## 10. 頁面 Controller

### 10.1 HomeController

```java
@GetMapping("/")
public String home() {
    return "home";
}
```

進入首頁，回傳 `templates/home.html`。

### 10.2 DoctorPageController

處理：

- `/doctors`
- `/doctors/{doctorId}`

`/doctors` 可以用 query parameter 篩選科別：

```text
/doctors?department=內科
```

重點：

```java
@RequestParam(required = false) String department
```

`required = false` 表示這個參數可有可無。

`/doctors/{doctorId}` 使用：

```java
@PathVariable String doctorId
```

代表從網址路徑取得值。

### 10.3 PatientController

有兩種功能：

```java
@GetMapping("/patients")
public String showPatients(Model model)
```

回傳 HTML 頁面。

```java
@GetMapping("/api/patients")
@ResponseBody
public List<Patient> getPatientsApi()
```

回傳 JSON。

`@ResponseBody` 的意思是不要找 HTML，直接把回傳值轉成 HTTP response body。

### 10.4 AppointmentPageController

```java
@GetMapping("/appointments")
public String listAppointments(Model model)
```

查全部掛號，顯示 `appointments.html`。

### 10.5 StatsController

統計：

- 醫師總數
- 病患總數
- 掛號總數
- 依科別統計掛號數

程式使用：

```java
appointmentCountsByDepartment.merge(department, 1L, Long::sum);
```

意思是：

- 如果這個科別還不存在，就放入 1
- 如果已經存在，就加 1

---

## 11. REST API Controller

### 11.1 HealthController

API：

```text
GET /api/health
GET /api/about
```

回傳 JSON。

`Map.of(...)` 可以快速建立 JSON 結構。

### 11.2 DoctorApiController

API：

| Method | 路徑 | 功能 |
|---|---|---|
| GET | `/api/doctors` | 查全部醫師 |
| GET | `/api/doctors?department=內科` | 依科別查醫師 |
| GET | `/api/doctors/{doctorId}` | 查單一醫師 |
| GET | `/api/departments` | 查所有科別 |
| POST | `/api/doctors` | 新增醫師 |
| PUT | `/api/doctors/{doctorId}` | 修改醫師 |
| DELETE | `/api/doctors/{doctorId}` | 刪除醫師 |

### 11.3 ResponseEntity

`ResponseEntity` 可以控制：

- HTTP 狀態碼
- Response body

例子：

```java
return ResponseEntity.ok(d);
```

代表 200 OK。

```java
return ResponseEntity.notFound().build();
```

代表 404 Not Found。

```java
return ResponseEntity.status(201).body(saved);
```

代表 201 Created。

```java
return ResponseEntity.noContent().build();
```

代表 204 No Content。

### 11.4 AppointmentApiController

API：

| Method | 路徑 | 功能 |
|---|---|---|
| GET | `/api/appointments` | 查全部掛號 |
| GET | `/api/appointments?date=2026-05-01` | 依日期查掛號 |
| GET | `/api/appointments?doctorId=D001` | 依醫師查掛號 |
| GET | `/api/appointments/count` | 掛號總數 |
| PUT | `/api/appointments/{apptId}/status` | 修改掛號狀態 |

修改狀態時會檢查：

1. 是否有這筆掛號
2. 登入醫師是否是這筆掛號的醫師
3. 新狀態是否合法

合法狀態：

```text
BOOKED
COMPLETED
CANCELLED
```

---

## 12. 掛號流程

### 12.1 GET 顯示掛號表單

```java
@GetMapping("/appointment/new")
public String newAppointmentForm(Model model)
```

做的事：

1. 建立 `AppointmentForm`
2. 預設日期為今天
3. 查出所有醫師
4. 回傳 `appointment-new.html`

### 12.2 POST 送出掛號

```java
@PostMapping("/appointment/new")
public String submitAppointment(
    @Valid @ModelAttribute("form") AppointmentForm form,
    BindingResult result,
    Model model)
```

完整流程：

```text
使用者填表單
-> POST /appointment/new
-> @Valid 驗證 AppointmentForm
-> 如果驗證失敗，回 appointment-new.html
-> 用 chartNo 查 Patient
-> 用 doctorId 查 Doctor
-> 找不到病患或醫師，回表單並顯示錯誤
-> 建立 Appointment
-> 設定 patient、doctor、apptDate、timeSlot、status
-> appointmentRepo.save(appt)
-> 回 appointment-result.html
```

### 12.3 為什麼需要 AppointmentForm？

表單傳來的是字串：

```text
chartNo = TEST00001
doctorId = D001
apptDate = 2026-05-01
timeSlot = AM
```

但 Entity `Appointment` 需要的是物件：

```java
private Patient patient;
private Doctor doctor;
private LocalDate apptDate;
```

所以要先用 `AppointmentForm` 接收表單字串，再去資料庫查出真正的 `Patient` 和 `Doctor`。

---

## 13. Bean Validation 表單驗證

本專案用：

```java
@Valid
BindingResult result
```

`@Valid` 啟動驗證。

`BindingResult` 接住驗證錯誤。

### 13.1 AppointmentForm 驗證

```java
@NotBlank(message = "請輸入病歷號")
@Pattern(regexp = "TEST\\d{5}", message = "病歷號格式為 TESTxxxxx")
private String chartNo;
```

意思：

- 不能空白
- 格式必須是 `TEST` 加 5 個數字

例子：

```text
TEST00001 正確
ABC00001 錯誤
TEST01 錯誤
```

### 13.2 LoginForm 驗證

```java
@Pattern(regexp = "D\\d{3}", message = "醫師編號格式錯誤")
private String doctorId;
```

意思是醫師編號格式必須是 `D` 加 3 個數字。

例子：

```text
D001 正確
D01 錯誤
A001 錯誤
```

### 13.3 常考錯誤

`BindingResult` 必須緊接在被驗證的參數後面。

正確：

```java
public String login(@Valid @ModelAttribute LoginForm form,
                    BindingResult result,
                    HttpSession session)
```

錯誤：

```java
public String login(@Valid @ModelAttribute LoginForm form,
                    HttpSession session,
                    BindingResult result)
```

---

## 14. 登入、Session、登出

### 14.1 HTTP 是無狀態的

HTTP 本身不記得上一個請求。

所以如果要讓伺服器知道「這個人已經登入」，需要 Session。

### 14.2 Session 是什麼？

Session 是存在伺服器端的資料。

瀏覽器會保存一個 Session ID，通常放在 Cookie 裡。之後每次請求會帶上這個 Session ID，伺服器就知道是哪個使用者。

### 14.3 LoginController 登入流程

```text
GET /login
-> 顯示 login.html

POST /login
-> 驗證 LoginForm
-> 用 doctorId 查 Doctor
-> 用 BCrypt.checkpw 檢查密碼
-> 成功後把 doctorId 和 name 放進 Session
-> redirect:/dashboard
```

登入成功後存入：

```java
session.setAttribute("loggedInDoctorId", doctor.getDoctorId());
session.setAttribute("loggedInDoctorName", doctor.getName());
```

### 14.4 登出流程

```java
@PostMapping("/logout")
public String logout(HttpSession session) {
    session.invalidate();
    return "redirect:/login";
}
```

`session.invalidate()` 代表清除 Session。

---

## 15. BCrypt 密碼安全

### 15.1 為什麼不能存明碼密碼？

如果資料庫被偷，明碼密碼會直接外洩。

所以密碼不能存：

```text
12345678
```

應該存雜湊：

```text
$2a$10$XhyEgd4qh5TXJa7NkMg3gOqsJxATykAyJERH7ZqTD7eEPVlcmgewm
```

### 15.2 BCrypt 用法

檢查密碼：

```java
BCrypt.checkpw(form.getPassword(), doctor.getPasswordHash())
```

產生新密碼雜湊：

```java
BCrypt.hashpw(form.getNewPassword(), BCrypt.gensalt())
```

### 15.3 BCrypt 常考

同一個密碼每次雜湊結果可能不一樣，因為 BCrypt 會加 salt。

驗證時不是直接比較字串，而是用：

```java
BCrypt.checkpw(明碼, 雜湊)
```

---

## 16. Interceptor 權限保護

### 16.1 Interceptor 是什麼？

Interceptor 會在 Controller 執行前攔截請求。

本專案用 `LoginRequiredInterceptor` 檢查使用者是否登入。

### 16.2 LoginRequiredInterceptor

重點：

```java
Object loggedIn = session.getAttribute("loggedInDoctorId");

if (loggedIn == null) {
    ...
    return false;
}

return true;
```

如果沒有登入：

- API 請求回 401 JSON
- 一般頁面 redirect 到 `/login`

### 16.3 WebConfig 註冊 Interceptor

```java
registry.addInterceptor(loginInterceptor)
    .addPathPatterns(
        "/dashboard",
        "/dashboard/**",
        "/api/auth/me",
        "/api/appointments/*/status",
        "/password",
        "/password/**"
    )
    .excludePathPatterns(
        "/login",
        "/logout"
    );
```

意思是這些路徑需要登入：

- `/dashboard`
- `/api/appointments/*/status`
- `/password`

不需要登入：

- `/login`
- `/logout`

---

## 17. Dashboard 流程

`DashboardController`：

```text
GET /dashboard
-> 從 Session 取得 loggedInDoctorId
-> 查 Doctor
-> 如果查不到，清除 Session 並回 login
-> 取得今天日期
-> 查這位醫師今天的掛號
-> 回 dashboard.html
```

查詢方法：

```java
appointmentRepo.findByDoctorAndApptDate(doctor, today)
```

Dashboard 頁面中，按「取消」會用 JavaScript 發 API：

```javascript
fetch('/api/appointments/' + button.dataset.id + '/status', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ status: 'CANCELLED' })
})
```

這是前端用 `fetch` 呼叫 REST API。

---

## 18. 修改密碼流程

`PasswordController`：

```text
GET /password
-> 顯示修改密碼頁

POST /password
-> 從 Session 取得 doctorId
-> 查 Doctor
-> 檢查舊密碼
-> 檢查新密碼至少 8 字元
-> 檢查確認密碼一致
-> BCrypt 產生新雜湊
-> doctorRepo.save(doctor)
```

考點：

- 修改密碼前必須確認舊密碼
- 新密碼不能太短
- 不能直接存明碼
- 修改後要 `save`

---

## 19. 專案所有主要路由整理

### 19.1 頁面路由

| 路徑 | Controller | View | 功能 |
|---|---|---|---|
| `/` | `HomeController` | `home.html` | 首頁 |
| `/doctors` | `DoctorPageController` | `doctors.html` | 醫師清單 |
| `/doctors/{doctorId}` | `DoctorPageController` | `doctor-detail.html` | 醫師詳細 |
| `/patients` | `PatientController` | `patients.html` | 病患清單 |
| `/appointment/new` GET | `AppointmentController` | `appointment-new.html` | 顯示掛號表單 |
| `/appointment/new` POST | `AppointmentController` | `appointment-result.html` | 送出掛號 |
| `/appointments` | `AppointmentPageController` | `appointments.html` | 掛號紀錄 |
| `/stats` | `StatsController` | `stats.html` | 統計資訊 |
| `/login` GET | `LoginController` | `login.html` | 登入頁 |
| `/login` POST | `LoginController` | redirect | 處理登入 |
| `/logout` POST | `LoginController` | redirect | 登出 |
| `/dashboard` | `DashboardController` | `dashboard.html` | 醫師 Dashboard |
| `/password` | `PasswordController` | `password.html` | 修改密碼 |

### 19.2 API 路由

| Method | 路徑 | 功能 |
|---|---|---|
| GET | `/api/health` | 健康檢查 |
| GET | `/api/about` | 專案資訊 |
| GET | `/api/doctors` | 所有醫師 |
| GET | `/api/doctors?department=內科` | 依科別查醫師 |
| GET | `/api/doctors/{doctorId}` | 查單一醫師 |
| GET | `/api/departments` | 所有科別 |
| POST | `/api/doctors` | 新增醫師 |
| PUT | `/api/doctors/{doctorId}` | 修改醫師 |
| DELETE | `/api/doctors/{doctorId}` | 刪除醫師 |
| GET | `/api/patients` | 所有病患 |
| GET | `/api/appointments` | 所有掛號 |
| GET | `/api/appointments?date=2026-05-01` | 依日期查掛號 |
| GET | `/api/appointments?doctorId=D001` | 依醫師查掛號 |
| GET | `/api/appointments/count` | 掛號總數 |
| PUT | `/api/appointments/{apptId}/status` | 修改掛號狀態 |

---

## 20. 必背註解整理

### 20.1 Spring MVC

| 註解 | 用途 |
|---|---|
| `@Controller` | 回傳 HTML 頁面 |
| `@RestController` | 回傳 JSON |
| `@GetMapping` | 處理 GET 請求 |
| `@PostMapping` | 處理 POST 請求 |
| `@PutMapping` | 處理 PUT 請求 |
| `@DeleteMapping` | 處理 DELETE 請求 |
| `@RequestParam` | 取得 query parameter |
| `@PathVariable` | 取得 URL 路徑變數 |
| `@RequestBody` | 讀取 JSON body |
| `@ModelAttribute` | 綁定表單資料 |
| `@ResponseBody` | 直接回傳 response body |
| `@Autowired` | 依賴注入 |

### 20.2 JPA

| 註解 | 用途 |
|---|---|
| `@Entity` | 類別對應資料表 |
| `@Table` | 指定資料表名稱 |
| `@Id` | 主鍵 |
| `@Column` | 欄位 |
| `@GeneratedValue` | 自動產生主鍵 |
| `@ManyToOne` | 多對一關聯 |
| `@JoinColumn` | 外鍵 |
| `@Convert` | 型別轉換 |
| `@Query` | 自訂查詢 |

### 20.3 Validation

| 註解 | 用途 |
|---|---|
| `@Valid` | 啟動驗證 |
| `@NotBlank` | 不可空白 |
| `@Pattern` | 正規表達式格式限制 |

### 20.4 其他

| 註解 | 用途 |
|---|---|
| `@SpringBootApplication` | Spring Boot 入口 |
| `@Configuration` | 設定類別 |
| `@Component` | Spring 元件 |
| `@JsonIgnore` | JSON 不輸出該欄位 |

---

## 21. 重要觀念比較

### 21.1 `@RequestParam` vs `@PathVariable`

`@RequestParam` 用在 query string：

```text
/doctors?department=內科
```

```java
@RequestParam String department
```

`@PathVariable` 用在網址路徑：

```text
/doctors/D001
```

```java
@PathVariable String doctorId
```

### 21.2 `@RequestBody` vs `@ModelAttribute`

| 註解 | 用途 |
|---|---|
| `@RequestBody` | 接 JSON |
| `@ModelAttribute` | 接 HTML 表單 |

例子：

```java
@RequestBody Doctor doctor
```

適合：

```json
{
  "doctorId": "D006",
  "name": "吳大明醫師"
}
```

```java
@ModelAttribute AppointmentForm form
```

適合 HTML form。

### 21.3 redirect vs return view

```java
return "login";
```

代表直接顯示 `login.html`。

```java
return "redirect:/dashboard";
```

代表請瀏覽器重新發請求到 `/dashboard`。

登入成功通常用 redirect，避免重新整理時重複送出表單。

---

## 22. 安全觀念

### 22.1 SQL Injection

攻擊者在輸入欄位放 SQL 片段，試圖破壞查詢。

本專案使用 JPA Repository，通常不直接拼接 SQL，因此比手寫字串 SQL 安全。

危險寫法：

```java
"select * from doctor where id = '" + userInput + "'"
```

### 22.2 XSS

攻擊者輸入 JavaScript，讓別人的瀏覽器執行。

Thymeleaf 的 `th:text` 預設會 escape HTML，比直接輸出安全。

### 22.3 CSRF

使用者已登入時，被其他網站誘導送出惡意請求。

本專案手寫 Session 登入，但沒有完整 Spring Security CSRF 保護。考試如果問安全改進，可以回答：

- 使用 Spring Security
- 加 CSRF token
- 限制重要操作只能 POST/PUT/DELETE
- 加上權限檢查

### 22.4 醫療資料安全

醫療資料屬於敏感個資。

基本原則：

- 資料最小化
- 不顯示不必要的病患資訊
- 密碼不可明碼儲存
- API 不應暴露密碼雜湊
- 重要操作要確認登入與權限
- 錯誤訊息不要洩漏太多資訊

---

## 23. 可能考題與答案模板

### 題目 1：請說明 Spring Boot 專案啟動流程。

答案：

Spring Boot 專案從 `MiniclinicApplication` 的 `main()` 方法開始執行。`SpringApplication.run(...)` 會啟動 Spring Boot 應用程式，並啟動內嵌 Tomcat。`@SpringBootApplication` 會啟用自動設定與元件掃描，讓 Controller、Repository、Interceptor 等 Spring 元件可以被建立與使用。

### 題目 2：請說明 MVC 架構在 MiniClinic 中如何運作。

答案：

MVC 分成 Model、View、Controller。MiniClinic 的 `Doctor`、`Patient`、`Appointment` 是 Model；`doctors.html`、`appointment-new.html` 是 View；`DoctorPageController`、`AppointmentController` 是 Controller。以 `/doctors` 為例，Controller 接收請求後透過 `DoctorRepository` 查詢資料，將資料放進 `Model`，最後回傳 `doctors.html` 由 Thymeleaf 顯示畫面。

### 題目 3：`@Controller` 和 `@RestController` 差在哪裡？

答案：

`@Controller` 通常用來回傳 HTML 頁面名稱，例如 `return "doctors"` 會對應到 `templates/doctors.html`。`@RestController` 則會直接把方法回傳值轉成 JSON，適合 REST API，例如 `/api/doctors` 回傳醫師清單 JSON。

### 題目 4：`@RequestParam` 和 `@PathVariable` 差在哪裡？

答案：

`@RequestParam` 取得 query string 的參數，例如 `/doctors?department=內科` 中的 `department`。`@PathVariable` 取得網址路徑中的變數，例如 `/doctors/D001` 中的 `D001`。

### 題目 5：請說明掛號送出流程。

答案：

使用者先用 `GET /appointment/new` 取得掛號表單。送出後瀏覽器發送 `POST /appointment/new`，Controller 使用 `@Valid @ModelAttribute("form") AppointmentForm` 接收並驗證表單。如果驗證失敗，回到表單頁。若成功，系統用病歷號查 `Patient`，用醫師編號查 `Doctor`，建立 `Appointment`，設定日期、時段與狀態 `BOOKED`，最後用 `appointmentRepo.save(appt)` 存入資料庫，並顯示結果頁。

### 題目 6：為什麼需要 `AppointmentForm`，不能直接用 `Appointment`？

答案：

HTML 表單傳來的是字串，例如 `chartNo`、`doctorId`、`apptDate`，但 `Appointment` Entity 需要的是 `Patient` 物件、`Doctor` 物件和 `LocalDate`。因此先用 `AppointmentForm` 接收表單資料，再用 Repository 查出真正的 Entity 物件，最後建立 `Appointment` 存入資料庫。

### 題目 7：什麼是 JPA Entity？

答案：

JPA Entity 是對應資料庫資料表的 Java 類別。類別上使用 `@Entity`，可以用 `@Table` 指定資料表名稱，欄位用 `@Column` 對應資料表欄位，主鍵用 `@Id`。MiniClinic 中的 `Doctor` 對應 `doctor` 表，`Patient` 對應 `patient` 表，`Appointment` 對應 `appointment` 表。

### 題目 8：`JpaRepository` 有什麼用途？

答案：

`JpaRepository` 提供常用資料庫操作方法，例如 `findAll()`、`findById()`、`save()`、`deleteById()`、`count()`。Spring Data JPA 也可以根據方法名稱自動產生查詢，例如 `findByDepartment` 可以依科別查醫師。

### 題目 9：請說明 `@ManyToOne` 在 Appointment 中的用途。

答案：

`Appointment` 中的 `@ManyToOne` 表示多筆掛號可以對應同一位醫師或同一位病患。`@JoinColumn(name = "doctor_id")` 表示用 `doctor_id` 作為外鍵連到 doctor 表；`@JoinColumn(name = "chart_no")` 表示用 `chart_no` 連到 patient 表。

### 題目 10：為什麼本專案需要 `LocalDateConverter`？

答案：

SQLite 沒有標準的日期型別，因此本專案用 `LocalDateConverter` 將 Java 的 `LocalDate` 轉成字串存入資料庫，例如 `2026-05-01`，讀取時再把字串轉回 `LocalDate`。

### 題目 11：請說明登入流程。

答案：

使用者先進入 `GET /login` 顯示登入頁。送出表單後，`POST /login` 用 `LoginForm` 接收醫師編號與密碼，並用 `@Valid` 驗證格式。接著用 `doctorRepo.findById()` 查詢醫師，再用 `BCrypt.checkpw()` 比對密碼。如果成功，將 `loggedInDoctorId` 和 `loggedInDoctorName` 放進 Session，最後 redirect 到 `/dashboard`。

### 題目 12：Session 是什麼？為什麼需要 Session？

答案：

HTTP 是無狀態的，每次請求彼此獨立，伺服器不會自動知道使用者是否登入。Session 是伺服器端保存的狀態資料。登入成功後，系統把醫師編號存入 Session，之後請求就可以透過 Session 判斷使用者是否已登入。

### 題目 13：Interceptor 在本專案的作用是什麼？

答案：

Interceptor 會在 Controller 執行前先攔截請求。本專案的 `LoginRequiredInterceptor` 會檢查 Session 中是否有 `loggedInDoctorId`。如果沒有登入，一般頁面會被導向 `/login`，API 請求則回傳 401 JSON 錯誤。這樣可以保護 `/dashboard`、`/password` 和修改掛號狀態的 API。

### 題目 14：為什麼密碼要用 BCrypt？

答案：

密碼不能用明碼儲存在資料庫，否則資料庫外洩時密碼會直接被看到。BCrypt 是密碼雜湊演算法，會加 salt，讓同一個密碼每次產生的雜湊可能不同。登入時使用 `BCrypt.checkpw(明碼, 雜湊)` 檢查密碼是否正確。

### 題目 15：`@JsonIgnore` 的用途是什麼？

答案：

`@JsonIgnore` 可以避免欄位出現在 JSON 回應中。本專案在 `Doctor.passwordHash` 上加 `@JsonIgnore`，避免 `/api/doctors` 回傳醫師資料時把密碼雜湊一起傳出去。

### 題目 16：請說明修改掛號狀態 API 的權限檢查。

答案：

`PUT /api/appointments/{apptId}/status` 會先從 Session 取得登入醫師的 `loggedInDoctorId`。接著查詢該掛號是否存在，如果不存在回 404。若存在，會檢查這筆掛號的醫師編號是否等於登入醫師編號。如果不是自己的掛號，回 403。最後才檢查狀態是否合法並儲存。

### 題目 17：請說明 `data.sql` 的用途。

答案：

`data.sql` 用來在應用程式啟動時初始化資料庫資料。本專案使用它新增初始醫師、病患和掛號資料。因為 `spring.sql.init.mode=always`，所以每次啟動都會執行。為了避免重複插入造成主鍵衝突，使用 `INSERT OR IGNORE`。

### 題目 18：`redirect:/login` 和 `return "login"` 差在哪裡？

答案：

`return "login"` 是直接回傳 `login.html` 模板。`redirect:/login` 則是叫瀏覽器重新發送請求到 `/login`。登入成功或登出後通常用 redirect，避免重新整理頁面時重複提交表單。

### 題目 19：請說明 MiniClinic 的資料表關係。

答案：

MiniClinic 有三張主要資料表：`doctor`、`patient`、`appointment`。`doctor` 儲存醫師資料，`patient` 儲存病患資料，`appointment` 儲存掛號資料。每筆掛號會透過 `doctor_id` 連到一位醫師，透過 `chart_no` 連到一位病患。因此醫師和掛號是一對多，病患和掛號也是一對多。

### 題目 20：如果要改進本專案安全性，你會怎麼做？

答案：

可以使用 Spring Security 取代手寫登入，加入 CSRF token，對不同角色設定權限，避免未授權使用者操作資料。也可以加入審計紀錄，記錄誰在什麼時間修改了掛號或病患資料。醫療資料應遵守資料最小化原則，不顯示不必要的個資。

---

## 24. 考前快速背誦版

### 24.1 專案一句話

MiniClinic 是使用 Spring Boot、Thymeleaf、JPA、SQLite、Session 和 Interceptor 製作的簡易診所掛號系統。

### 24.2 MVC

```text
Controller 接收請求
Repository 查資料
Model 傳資料
Thymeleaf 顯示畫面
```

### 24.3 JPA

```text
Entity = Java 類別對應資料表
Repository = 操作資料庫
JpaRepository = 提供 findAll、findById、save、deleteById
```

### 24.4 登入

```text
表單送出
查醫師
BCrypt 檢查密碼
成功後存 Session
進 dashboard
```

### 24.5 權限

```text
Interceptor 檢查 Session
沒有 loggedInDoctorId 就擋下
API 回 401
頁面導向 /login
```

### 24.6 掛號

```text
AppointmentForm 接表單
查 Patient
查 Doctor
建立 Appointment
狀態 BOOKED
save 到資料庫
```

---

## 25. 你最需要先搞懂的 10 個字

1. **Controller**：接收網址請求的人。
2. **View**：HTML 畫面。
3. **Model**：Controller 傳給 HTML 的資料。
4. **Entity**：對應資料表的 Java 類別。
5. **Repository**：幫你查資料庫的介面。
6. **Session**：伺服器用來記住登入狀態。
7. **Interceptor**：在進 Controller 前先檢查請求。
8. **BCrypt**：安全儲存密碼的雜湊工具。
9. **Thymeleaf**：把 Java 資料放進 HTML 的模板引擎。
10. **REST API**：回傳 JSON 給程式使用的網址。

---

## 26. 建議讀書順序

如果你目前真的完全不會，建議照這個順序讀：

1. 先讀第 1、2、3、4 章，理解 Web、專案結構、MVC。
2. 再讀第 5、10、19 章，知道頁面怎麼出現。
3. 再讀第 6、7、8、9 章，理解資料庫和 JPA。
4. 再讀第 12、13 章，理解表單和掛號流程。
5. 最後讀第 14、15、16、17、18、22 章，理解登入和安全。
6. 考前直接背第 23、24、25 章。

---

## 27. 口試或手寫時可以用的總結

MiniClinic 是一個 Spring Boot MVC 專案。使用者透過瀏覽器送出請求，Controller 接收後透過 Repository 操作 SQLite 資料庫，再把資料放到 Model 給 Thymeleaf 顯示。資料庫部分使用 JPA Entity 對應 `doctor`、`patient`、`appointment` 三張表，並透過 `@ManyToOne` 表示掛號和醫師、病患之間的多對一關係。登入功能使用 Session 記住登入醫師，密碼使用 BCrypt 雜湊儲存，並透過 Interceptor 保護 Dashboard、修改密碼和修改掛號狀態等需要登入的路由。

